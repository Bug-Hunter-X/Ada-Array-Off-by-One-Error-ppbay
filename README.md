# Ada Array Off-by-One Error

This repository demonstrates a common off-by-one error in Ada that can occur when working with arrays and their ranges.  Ada's strong typing and range checking can help prevent this error, but it's important to understand how ranges work to avoid such problems.  The `bug.ada` file contains code illustrating the error. The solution is provided in `bugSolution.ada`.